{% extends 'base_front.html.twig' %}

{% block title %}Nouveau cadeau {% endblock %}

{% block body %}
<div class="flex flex-center">
    <h1>Ajouter un cadeau </h1>
</div>


<div class="flex flex-center">
    <h2>
        <a href="{{path('front_add_gift_via_link',{'id': gift_list.id} )}}">Ajouter un cadeau avec son lien</a>
    </h2>
</div>


<div class="flex flex-center">
    <button>
    <a href="{{ path('front_app_gift_index') }}">back to list</a>
    </button>
</div>


{{ include('front/gift/_form.html.twig') }}



{% endblock %}